expect = chai.expect
Loader = require '../../app/loader'
Sys = require '../../app/gamesys'
utils = require './utils'

describe 'timer class test', ->
  sys = new Sys 640, 480
  loader = new Loader sys


  before ->

  after ->

  beforeEach ->

  afterEach ->
